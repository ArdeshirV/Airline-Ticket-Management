version: "3.9"

services:
  postgres:
    image: postgres:13.11
    hostname: ${DATABASE_HOST}
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - "5432:5432"
    restart: always
    volumes:
      - aireplae-postgres:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4:latest
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_MAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_PW}
    ports:
      - "5050:80"
    restart: always
    volumes:
      - aireplae-pgadmin:/var/lib/pgadmin

  backend:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: "./wait-for-it.sh ${DATABASE_HOST}:${DATABASE_PORT} -- ./main"
    depends_on:
      - postgres
    hostname: backend
    working_dir: /root/
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - DATABASE_HOST=${DATABASE_HOST}
      - DATABASE_PORT=${DATABASE_PORT}
    ports:
      - "8080:8080"
      - "8081:8081"
    volumes:
      - aireplae-backend:/root/

  # proxy:
  #   image: nginx
  #   volumes:
  #     - type: bind
  #       source: ./proxy/nginx.conf
  #       target: /etc/nginx/conf.d/default.conf
  #       read_only: true
  #   ports:
  #     - 80:80
  #   depends_on:
  #     - backend

volumes:
  aireplae-postgres:
  aireplae-pgadmin:
  aireplae-backend:
